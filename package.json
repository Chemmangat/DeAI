{
  "name": "ai-slop-detector-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:detector && npm run build:extension",
    "build:detector": "cd packages/detector && npm run build",
    "build:extension": "cd packages/extension && npm run build"
  }
}
